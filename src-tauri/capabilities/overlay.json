{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Permissoes minimas da janela overlay (hot path).",
  "windows": ["overlay"],
  "permissions": [
    "core:default"
  ]
}
